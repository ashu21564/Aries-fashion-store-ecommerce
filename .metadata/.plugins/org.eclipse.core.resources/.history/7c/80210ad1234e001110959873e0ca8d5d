<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" isELIgnored="false" %>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>

<!-- CSS -->
<link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/style.css">
<link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/home.css">

<!-- NAVBAR -->
<%@ include file="partials/navbar.jsp" %>

<!-- ================= HERO SECTION (UPDATED) ================= -->
<div class="hero">

    <!-- 🔥 BANNER IMAGE -->
    <img src="${pageContext.request.contextPath}/assets/images/banner.jpg" class="hero-img"/>

    <!-- 🔥 OVERLAY TEXT -->
    <div class="hero-content">
        <h1>Aries Fashion Store</h1>
        <p>Bold. Elegant. Unapologetic.</p>
    </div>

</div>

<!-- ================= CATEGORY SECTION ================= -->
<div class="container category-section">

    <h2>Shop by Category</h2>

    <div class="categories">

        <c:if test="${not empty categories}">
            <c:forEach var="c" items="${categories}">

                <a href="${pageContext.request.contextPath}/category?id=${c.categoryId}" class="category-card">

                    <!-- 🔥 ICON IMAGE -->
                    <img src="${pageContext.request.contextPath}/assets/images/${c.categoryName}.png" />

                    <p>${c.categoryName}</p>

                </a>

            </c:forEach>
        </c:if>

        <c:if test="${empty categories}">
            <p>No categories available</p>
        </c:if>

    </div>

</div>

<!-- ================= PRODUCT SECTION ================= -->
<div class="container products-section">

    <h2>Latest Products</h2>

    <div class="products-grid">

        <c:if test="${not empty products}">
            <c:forEach var="p" items="${products}">
                <div class="product-card">

                    <a href="${pageContext.request.contextPath}/product-details?id=${p.productId}">

                        <img src="${pageContext.request.contextPath}/assets/images/${p.imageUrl}" 
                             alt="${p.productName}" />

                        <h3>${p.productName}</h3>

                    </a>

                    <p>${p.discountPercentage}% OFF</p>

                    <a href="${pageContext.request.contextPath}/product-details?id=${p.productId}">
                        <button class="btn">View</button>
                    </a>

                </div>
            </c:forEach>
        </c:if>

        <c:if test="${empty products}">
            <p>No products available</p>
        </c:if>

    </div>

</div>

<!-- FOOTER -->
<%@ include file="partials/footer.jsp" %>